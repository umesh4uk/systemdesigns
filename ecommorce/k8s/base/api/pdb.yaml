apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ecommerce-api-pdb
  labels:
    app: ecommerce-api
spec:
  minAvailable: 1          # always keep at least 1 pod up during node drain / upgrades
  selector:
    matchLabels:
      app: ecommerce-api
